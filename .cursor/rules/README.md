# Cursor Rules (Placeholder)

Add project-specific Cursor rules here (`.mdc` or host-supported formats).

This template ships empty so each project can define its own lint,
security, and style rules without inheriting unrelated product policy.
