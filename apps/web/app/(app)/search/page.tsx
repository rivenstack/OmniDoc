import { ContentRegion, PageHeader } from "@omnidoc/ui";

export default function SearchPage() {
  return (
    <ContentRegion>
      <PageHeader
        title="Search"
        description="Search is its own operation, separate from Ask. Results arrive with F-06."
      />
    </ContentRegion>
  );
}
