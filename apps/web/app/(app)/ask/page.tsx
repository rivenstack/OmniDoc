import { ContentRegion, PageHeader } from "@omnidoc/ui";

export default function AskPage() {
  return (
    <ContentRegion>
      <PageHeader
        title="Ask"
        description="Ask your notes. Answers are cited, and refusal or partial outcomes are successful responses, not errors. The composer arrives with F-07."
      />
    </ContentRegion>
  );
}
