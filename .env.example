# OmniDoc — web placeholders (S-01a)
#
# Copy to `.env.local` for local runs. NEVER commit real keys, BYOK
# material, or `.env.local` contents. Production AI remains gated
# (mock-first); these placeholders carry no secrets.

# Public base URL of the web app (local dev default).
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Base URL of the JVM API (S-01b health endpoint; not present yet).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# Runtime mode label near Ask (ADR-0004): mock | operator_free_tier | customer_key.
# S-01a scaffold default is mock. Live modes stay forbidden until the CX gate closes.
NEXT_PUBLIC_RUNTIME_MODE=mock

# Corpus label default for the portfolio demo path (sample vs mine).
NEXT_PUBLIC_CORPUS_LABEL=mine

# --- JVM API placeholders (S-01b / B-02) ---
# Copy relevant keys into the shell or IDE run config for `apps/api`.
# NEVER commit real OpenRouter / BYOK keys — production AI remains gated.

# HTTP listen port (matches application.yml ${API_PORT:8080}).
API_PORT=8080

# Spring profile label for local runs (local = Compose Postgres + Flyway).
SPRING_PROFILES_ACTIVE=local

# Optional heap override (bootRun already defaults to -Xmx256m for Free Tier RSS).
# JAVA_TOOL_OPTIONS=-Xmx256m

# --- Local Postgres 18 + pgvector (apps/api/compose.yaml) ---
# Placeholders only. Do not commit real passwords.
POSTGRES_PORT=5432
POSTGRES_SUPERUSER_PASSWORD=omnidoc_postgres_dev
OMNIDOC_DB=omnidoc
OMNIDOC_JDBC_URL=jdbc:postgresql://localhost:5432/omnidoc
OMNIDOC_MIGRATOR_USER=omnidoc_migrator
OMNIDOC_MIGRATOR_PASSWORD=omnidoc_migrator_dev
OMNIDOC_APP_USER=omnidoc_app
OMNIDOC_APP_PASSWORD=omnidoc_app_dev
# Tenant GUC (set via TenantRlsSession / set_config(..., true) inside a txn):
# app.current_tenant_id

# OpenRouter / customer BYOK — intentionally unset. Do not enable live modes here.
# OPENROUTER_API_KEY=
# CUSTOMER_BYOK_KEY=
