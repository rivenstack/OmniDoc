# docs/context.md

Optional addendum or mirror of root `context.md`.

**Convention for OmniDoc:** keep the **authoritative** live status in
the repository-root `context.md`. Use this file only if the project
needs a docs-tree summary or historical addendum.

If both exist and conflict, root `context.md` wins unless Commander
explicitly documents otherwise.
